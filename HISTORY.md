# HISTORY — slack-sandbox 개발 기록 (v2.2)

**Jira·Notion 없이 Slack 하나로 — LLM(Claude)이 프로젝트 비서가 되는 AX 환경.** 사람은 Slack만 보고, 기록은 밑단 GitHub에 쌓인다.

무엇을 왜 만들었고, 무엇을 버렸고, 무엇이 불안한지를 적는다. 검토(어드바이스)와 인수인계용.
기간: 2026-09-18 ~ 2026-09-19 · 개발: Claude(이동원 지시) · 커밋 77건 · 판단일 2026-10-02

## 1. 한 장 요약 (2026-09-19 오후 기준)
| 칸 | 내용 (`project.md`) |
| --- | --- |
| Pain point | 도구가 대화 밖에 있다 · PM이 비서 일을 한다 · 맥락이 흩어진다 · AI가 팀을 안 굴린다 |
| 목표 | Slack 안의 AI 프로젝트 비서로, 프로젝트 팀의 협업 생산성을 2배로 |
| 할 일 → 성공 기준 | 구체화 → 완료 조건 갖춘 이슈 80% · 나눔 → AI 배정 수락률 70% · 챙김 → 3일 넘게 멈춘 일 0 · 남김 → 인수인계 성공 1건 |
| 기능 (상위 이슈) | 요청 입구 · 질문으로 구체화 · 이슈 정리·보완 · 담당 추천 · 우선순위·과부하 조절 · 알림·후속 · 회의 운영 · 동기화·기록 · 인수인계 · 보기 · 기반 |
| 로드맵 | ✅ 기획(9/18) → ✅ 프로토타입(9/19) → ▶ 개발·도그푸딩(~9/24) → PoC 우리 팀 1~3명(~10/01) → 판단(10/02) → 파일럿 외부 팀(미정) → 본사업 |
| 원칙 | AI는 돕고 정하는 건 사람 · 작업자에게 틀을 요구하지 않는다 (한 줄 쓰고, 답하고, 👍) · 사람이 고친 값은 AI가 덮어쓰지 않는다 |

## 2. 지금 화면
| 어디 | 무엇 |
| --- | --- |
| **#proj-request** | 요청 입구 — 🎫 한 줄 · 메시지 단축키 · `/issue` |
| **#proj-sandbox 카드** (보기만) | `🔴 #11 제목` / `🕒 대기 · P1 · 담당 이동원 · 📅 9/23 · 🧩 기능` / [✋ 내가 할게요] [📄 상세] |
| **카드 스레드 ⚙️ 설정** (고르기) | 담당 · 우선순위 · 목표일 · 상태 · 로드맵 단계 · 기능 — AI가 먼저 채우고 이유를 적어 둠, 사람이 고침 |
| **📄 상세 팝업** | 목표와의 연결 · 이슈 정의 · 순서·담당 이유 · ✏️ 내용 수정(제목·정의·완료 조건) |
| **캔버스** (프로젝트 한눈에) | Pain point → 목표·할 일(진척·성공 기준) → 기능·연결 이슈 → 로드맵 → 누가 뭘 맡나 → 회의록. 약 7천 자 |
| **앱 홈** (sandbox-bot) | 내가 맡은 것 · 가져갈 수 있는 일 · 버튼 11개 (벤치마킹 · 기능별 · 단계별 · 우선순위별 · 담당 없는 일 · 작업 보드 · 이슈 목록 · 일정 · 회의 · 규칙 · 사용법) |
| **레포** | `project.md` 목표 · `roadmap.md` 단계 · `team.md` 역할 · `priority.md` 규칙 · `research/benchmark.md` 조사 · `docs/later-requirements.md` 나중 요건 · `templates/project-goal.md` 목표 틀 |

**용어 한 벌** (`priority.md` 0절): 우선순위 🔴 P1 긴급(사람만) · 🟠 P2 · 🟡 P3 · ⚪ P4 — 제목 앞 색 / 상태 🕒 대기 · 👀 진행 중 · ⛔ 보류 · ✅ 완료 · ❌ 취소 — 반응 이모지와 같은 뜻 / 📅 목표일(💡 = AI 제안).

## 3. 시간순
| 9/19 | 한 일 | 계기 |
| --- | --- | --- |
| 전날 | 해외 AX 사례 조사, Linear 폐기 이유 확인 (MCP 느림·유료) | 사용자 경험 |
| 00:10~00:49 | 무료 Slack · 채널 캔버스 API 갱신 성공 · `sync.py` · Socket Mode 봇 · `@정리` | 프로토타입 |
| 01:00~01:20 | GitHub 연동 (md 정본 · 이슈 · Projects 보드) · 상세 팝업 · 📌 회의록 | 무료 플랜 1년 뒤 삭제 대비 |
| 09:12~09:58 | 요청·이슈 채널 분리 · 입구 3개 · ✏️ 팝업 · `/meeting` | Linear Asks, 이모지 찾기 불편 |
| 10:28~11:47 | 누가 뭘 하나 · 역할표 AI 담당 추천 · 공개 계산식 우선순위 · 사람당 배정 한도 | 핵심 원칙 「누가 뭘 → 맞추기 → 다시 정하기」 |
| 11:59~12:03 | HISTORY v1 + 상위 모델 검토 (§7) | 소스 관리 점검 요청 |
| 12:18~12:21 | 캔버스를 한눈에 보기로, 다시 그리기 묶음·같으면 건너뜀 | 너무 길다 · 위 여백 |
| 12:34~12:49 | 목표·지표·로드맵 단계 연결, 단계 명칭 국내 용례로 (PoC → 파일럿 → 본사업) | 진행률 필요 · 네이버 뉴스 확인 |
| 13:18~13:54 | 목표 틀 v1.0~1.2 · Pain point · 할 일 4 · 기능 10 · 벤치마킹 문서화 | 「목표 · 무엇을 · 성공을 뭘로」 |
| 14:08~14:19 | 빠진 이슈 등록 · 이슈 10건 구체화 (라인·삼성 사례) · PM 순서 고정 · 나중 요건 | 기술 블로그 검토 |
| 14:24~14:53 | 카드 모양 여러 번 → Jira·Linear 방식 확정, 스레드 ⚙️ 설정, ✏️ 내용 수정, 조사 자료는 캔버스 밖 | 「복잡하다」 「보통 쓰는 방식」 |
| 15:00~15:35 | 요청 채널 정리 (봇이 대신 올린 17건 한 줄로, 앞으로 직접 등록은 카드만) · #31 대화로 구체화 (실제 대화로 시험 #47) · 연재 계획 (요즘IT 매주) · 채널에 「9/19 정리」 | 「스레드에서 대화하며 구체화」 (사용자) |
| 15:40~16:20 | 현황(`@sandbox-bot 현황` · 평일 9시) — 진행률·누가 뭘·위험 → 추천 [적용] 버튼, 이모지·세부 줄 걷어냄 · #35 `core.py` 분리 + 테스트 8개 (실데이터 43건 옛/새 차이 0) | 「누가 뭘 · 얼마나 남았나」 「위험이면 해결책까지」 「복잡하다」 |
| 23:30 | `/상태` 명령 · 전체 점검 — 안내 문구(❓ 사용법 · 캔버스 맨 위 · README)를 지금 방식으로 | 사용자 요청 |
| 9/19 23:50 · 9/20 00:05 | 검토 반영(봇 커밋은 그 파일만 · [적용]은 바뀐 카드만) · 위험·추천을 **일정만**으로 · 봇 말투를 친절하게 · #48 문구 템플릿 · #49 시작·완료 안내 · #50 다른 Slack 설치 등록 | 「언제까지 해야 하는데 안 된 것만」 「친절하게」 「통일성 · 다른 Slack」 |
| 9/20 00:10~00:40 | 백로그는 채널에 두기로 · 변경 이력 한 줄(사람·AI·회의) · 회의 결정 → 이슈 스레드 [적용] · #49 시작·완료 안내 + 중요한 변화 5가지 채널 한 줄 · 완료 조건 체크 → 카드 「2/4」 · 📆 날짜별 진행 | 「협업이라 진행 상황이 보여야」 「언제·뭐에 의해·왜를 한 줄로」 「날짜 단위로 계획대로 끝났나」 |
| 9/20 00:40~00:50 | #50 다른 Slack 설치 (config.json · `SANDBOX_DATA` · manifest.yaml · setup.py · docs/install.md, 새 팀 폴더로 설치·전체 흐름 시험) · #48 문구 템플릿 (`messages.py` + 말투 규칙 테스트, 팀별 `messages.json`) · 아이디어 보관함은 요건으로만 기록 | 「48·49·50 만들고 테스트까지」 |
| 9/20 00:50~01:00 | 완료 조건 체크박스를 스레드 ⚙️ 설정으로 (상세에도) · **🔍 확인 대기** 상태 — 담당자가 ✅·다 체크하면 요청 방에서 요청자(없으면 PM)를 태그해 [✅ 확인했어요] [↩️ 더 필요해요], 앱 홈 맨 위 · `/상태` · 다음 날 아침 다시 알림 | 「완료 승인이 눈에 띄어야」 「요청 방에 사람 태그」 |
| 9/20 01:00~13:00 | #30 나누기 6단계 끝(17파일·골든 2종) · 작업판 쓰기 줄이기(지문·10분·🔄 버튼) · 이름 정리(팀 대화방·프로젝트 방) · 설치 쉽게(앱 링크·토큰 묻기) · 봇 이름·얼굴을 메시지에 실어 보냄(설정 0) · 표정 5종 · **사고**: 자기 메시지 오인으로 카드 30개 → 고침·회귀 테스트·메시지 123개 삭제 · 작업 보고 통로 #56 · 이슈 검증·GitHub 규칙 설계 #58 | 「다른 Slack 에서 테스트」 「설정 없이 쓰게」 「이슈 관리가 안 된다」 |

## 4. 결정과 버린 안
| 결정 | 고른 안 | 버린 안 | 이유 |
| --- | --- | --- | --- |
| 이슈 도구 | Slack + GitHub Issues | Linear | Linear MCP가 느렸고 유료 (2026-06 폐기) |
| 정본 | 레포 md | Slack 메시지, 자체 DB | 무료 플랜 1년 뒤 삭제, AI가 파일을 바로 읽음 |
| AI 호출 | Claude 구독 `claude -p` | Anthropic API | 사용자 요청. 팀 상시 운용 약관은 확인 필요 |
| 캔버스 편집 | API 전체 replace, 5초 묶음·같으면 건너뜀 | 브라우저 조작 | 브라우저로 Enter·Backspace 입력 불가 |
| 담당 | AI 추천 → 본인 ✋/👍 확정 | AI 자동 확정 | 자율성 상실이 AI 거부감 원인 (HBR 2026-09) |
| 우선순위 계산 | AI는 숫자만, 계산은 코드 | AI가 순서까지 | 매번 흔들림(실측). 라인 기술 블로그도 같은 결론 |
| 우선순위·상태 표시 | Jira·Linear 방식 — 🔴P1~⚪P4 · 대기/진행 중/보류/완료/취소 · 📅 목표일 | 지금/다음/백로그/나중 구간 노출 | 구간이 오히려 헷갈림. 구간은 배정 한도(5개) 계산 안에서만 |
| P1 | 사람만 붙인다 | 점수로 | 점수로 매기니 P1 이 12건 — 긴급이 의미를 잃음 |
| 카드 | 보기만 (제목+요약+버튼 둘), 고르기는 스레드 ⚙️ | 카드에 드롭다운 4개 · ⋯ 메뉴 · 순서 이모지 | 목록이 복잡 (사용자) |
| 편집 권한 | 누구나 모든 칸 | 우선순위·단계·기능은 PM만 | 「Jira·Linear 처럼 자유롭게」 (사용자) |
| 삭제 | 하지 않고 ❌ 취소 (사유) | 하드 삭제 | 안 한 이유가 사라짐 |
| 채널 | 요청·이슈 2개 | 1개 | 사용자가 한 번 뒤집었다가 확정 |
| 목표 | AI 프로젝트 비서로 협업 생산성 2배 | 「Slack만으로 충분한가」 → 「AI 배정을 받아들이나」 | 사용자: 목표는 AX 환경 · 궁극은 협업 생산성 |
| 로드맵 단계 | 기획 → 프로토타입 → 개발·도그푸딩 → PoC → 판단 → 파일럿 → 본사업 | POC 를 개발 전에 · 파일럿 → PoC | 국내 표준 순서 (KT·SOA 기사 등) |
| 조사 자료 위치 | `research/benchmark.md` + 앱 홈 버튼 | 캔버스 아래에 붙이기 | 캔버스가 계속 길어짐 (사용자) |
| 백로그 위치 (9/20) | 지금처럼 이슈 채널에 카드로 둔다 | 담당 없는 일은 채널에서 빼고 목록으로 · 백로그 채널 분리 | 채널은 시간순이라 빼면 우선순위 높은 일이 안 보인다 (사용자). 대신 #49 에서 중요한 변화 5가지를 채널 맨 아래에 한 줄씩 (reply_broadcast) |
| 변경 이력·날짜별 기록 (9/20) | 요구사항·결정(담당·우선순위·목표일·단계)이 바뀌면 **사람이든 AI든 회의든** 카드 스레드에 **한 줄** 「🔄 9/20 목표일 9/23→9/26 · 이동원 (이유)」. 시작·완료·보류·회의도 같은 기록에 쌓여 📄 상세 · 앱 홈 📆 날짜별 진행 · 정본 md 「날짜별 기록」 · `/상태` 「지난 기록」 으로 본다. 완료 때 「계획대로 / N일 늦게 · 목표일 N번 바뀜」 | 조용히 덮어쓰기 · 이유 입력 강제 · 사람 변경만 기록 | 언제 · 왜 바뀌었는지 남아야 협업이 된다 (사용자). AI 는 배정 계산 사유를 그대로 적고, 사람은 선택 칸 — 우선순위·목표일·단계를 바꾸고 이유를 안 쓰면 스레드에서 한 번 묻는다. 새 카드의 첫 값은 변경이 아니라 남기지 않는다 |
| 완료 확인 (9/20) | 담당자가 끝내면 🔍 확인 대기 → 요청자(없으면 PM)가 요청 방에서 확인해야 완료. 팀 설정 `"review": false` 로 끌 수 있다 | 담당자 ✅ = 바로 완료 | 사람이 확인해야 끝난다 — 확인할 사람에게 잘 보여야 한다 (사용자). Jira·Linear 의 In Review 와 같은 자리 |
| 봇 이름·얼굴 (9/20) | 메시지마다 이름과 그림 주소를 함께 보낸다 (`chat:write.customize`) — 공개 저장소 webn77/pa-icons | 앱 설정에 올리기 · 워크스페이스 이모지 | 새 워크스페이스에서 **설정이 0**이 된다. 앱 아이콘만 한 번 올리면 된다 |
| GitHub 올리는 시점 (9/20) | **번호가 생기면 올린다** — 분류함(#51) 뒤에는 번호 = 사람이 정한 일 | 담당이 정해질 때 · 진행 중일 때 | 규칙이 한 줄이라 예외를 기억할 필요가 없다. 사람도 AI 도 「진행 중」 을 정확히 누르지 않는다 (사용자) |
| 검증 (9/20) | 모두플로2의 **방식만** 이식 — 막는 것/알려주는 것, 모르는 값은 걸린다 | 모두플로2 전면 이식 | 모두플로2는 「전체 상태 파일을 만들지 않는다」 가 헌법이고 우리는 cards.json 이 중심 — 상태 모델이 다르다. 정본을 md 로 옮기는 것은 다음 이슈 |
| 범용 요약·검색 | 만들지 않는다 (#6 취소) | #6 검색 · #17 요약 | Slackbot·Claude 가 이미 함. #17 은 「아침 알림」(챙김)으로 바꿔 다시 엶 |

## 5. 지금 구조
```
bot.py        1,761줄 · 함수 73개 — 이벤트, 카드·스레드 설정, 캔버스, 앱 홈·팝업, 회의, 배정, 우선순위, 지표
gh_link.py      281줄 — md 정본, git commit/push, GitHub 이슈·보드, 회의록·노트
cards.json      실행 상태(카드 42 · 회의 · 배정) — git 에 함께 올라감
project.md · roadmap.md · team.md · priority.md · canvas_head.md
research/benchmark.md · docs/later-requirements.md · templates/project-goal.md (v1.2)
issues/ · meetings/ · notes/   정본
토큰          ~/.config/slack-sandbox.env (레포 밖, 권한 600)
실행          맥에서 nohup 백그라운드 1개. 재부팅하면 꺼짐 (#10)
```
이슈: 42건 (#4~#46, 열림 33 · 완료 5 · 취소 4). 담당 — 이동원: #41 #11 #14 (P1) · #40 #25 / won7036: #10 #31 #35 (P1) · #38 #30.

## 6. 알려진 문제와 위험
1. **동기화 구멍** — GitHub 에 있는 이슈는 1건. 이슈 정의가 채워진 카드는 상태를 바꿀 때마다 GitHub 이슈가 하나씩 생긴다. 결함 A(커밋 범위)는 9/19 밤 고침 — 나머지 B~E 는 #30
2. **bot.py 비대** — 배정 계산은 `core.py` 로 분리하고 테스트 8개 (`python3 -m unittest discover -s tests -v`). 나머지는 아직 한 파일 (#35)
3. **상시 실행 없음** — 재부팅하면 봇이 꺼진다 → #10
4. **캔버스를 쓸 때마다 열어 둔 화면에 옛 칸이 쌓인다** — Slack 앱 쪽 문제라 봇이 없앨 수 없다. 9/20 실험: 통째 교체 → 열린 화면 칸 188→214(다시 열면 정상) · 칸 하나만 교체(section_id) → 표 모양이 깨져 서버에도 그대로 남음(쓰면 안 됨). 대책: 내용이 같으면 안 쓴다(지문 저장, 재시작해도) · 최소 10분 간격으로 모아서 · 바로 보고 싶으면 앱 홈 🔄 작업판 새로고침 · 이상하면 탭을 다시 열기. 9/20 재시작 20번 뒤 「작업판에 중복」 제보가 계기
5. **캔버스 제목 `####` 금지** — 하나라도 있으면 저장 전체 실패 (`canvas_editing_failed`, 실측)
6. **AI 추정 흔들림** — 새 이슈만 추정하게 막음. 목표일 💡 제안이 빠듯하게 나옴 (9/21~9/23)
7. **실사용자 0명** — 성공 기준이 전부 사람에 대한 것 → #41 PoC 참가자
8. **시험 데이터** — 가상 팀원 원본 메시지가 채널에 남음 → #38
9. **구독 약관** — 개인 구독으로 팀 봇 상시 운용은 확인 필요 → #16
10. **무료 플랜 한계** — 독립 캔버스·리스트·워크플로 없음, 2026-10-10 부터 1년 지난 메시지 삭제. Pro 로 풀리지만 「비용 0원」 논리가 약해짐

## 7. 다음 순서 (2026-09-20 21:00 기준)

**바로 다음: #66 여러 프로젝트** (사장님 지시 9/20 저녁) — 지금 봇은 프로젝트를 **하나만** 안다.
`config.json` 에 프로젝트 방·작업판·팀 대화방이 하나씩이고, 이슈 번호에 프로젝트 구분이 없다.
같은 Slack 앱으로 봇을 둘 띄우면 **둘 다 같은 앱 홈을 덮어쓴다** — 두 번째 프로젝트를 붙일 방법이 없다.
- 손댈 범위: `CHANNEL` 82줄 · `REQUEST` 20줄 · `CANVAS` 12줄, 파일 12개
- 설계 근거는 `research/benchmark.md` 「이슈 번호와 프로젝트 구분」 (Jira·GitHub·Linear 조사)
- 홈의 「프로젝트」 칸은 프로젝트가 하나라는 전제로 만든 것 — 여럿이면 다시 짜야 한다 (사장님 지적)

**오늘 끝낸 것**: #58 검증·동기화 · #60 번호의 주인을 GitHub 으로 · #57 정리 도우미(예방+치료)
· #54 어디서든 이슈 보기·만들기 · DM 열기 · 홈 다시 짜기(칸 45→12)

**그다음**: #52 제가 할게요 + 할 일 목록 · #53 이번 주 보기 · #55 산출물·완료 요청 (설계 `docs/request-flow.md`)
· #11 권한 3단계 문서 · #59 git 자동화 + 어긋남 대조 · #64 팀 기억 물어보기 · #65 search:read
**나중**: #51 분류함 (미룬 이유는 `docs/request-flow.md` 맨 아래) · #25 인수인계 시험

**PM 결정 기다리는 것**: #40 team.md 가용 시간 · #41 PoC 참가자 · #42 10/02 판단 회의
· 회사 승인 3가지(Slack 앱 설치 · 회사 대화를 Claude 로 · GitHub 위치) · #10 LaunchAgent 승인
**정할 것**: 대화 전체를 기록으로 남길지 (제 제안: 실시간 말고 하루 한 번 훑어 결정만 뽑기)
· 팀 데이터를 팀 레포로 (`docs/data-home.md`) · 매니페스트 소개 문구에 「GitHub 을 바탕으로」 넣을지

**오늘 새로 알게 된 것 (근거)**
- Slack 자체 AI(요약·검색·회의 받아쓰기·Slackbot MCP 클라이언트)는 대부분 Business+ 이상. 무료·Pro 팀엔 없다 → 우리 자리는 「팀 PM 비서의 판단」
- Slackbot MCP 클라이언트 = 앱 설정 Features → MCP Servers. 원격(HTTP) MCP 서버만 연결, 추가하면 `mcp:connect` 권한 + 재설치
- 호스팅 비용: Railway $5 · Fly.io $2~5 · Render $7 / Claude API Sonnet 5 $2·$10 per MTok → 3명 팀 월 $8~10 추정. 사용자는 회사 맥북 상시 실행을 택함
- 슬래시 명령은 한글 이름 가능(`/상태` 등록 확인). 내부 브라우저에선 Enter 가 안 먹어 전송 버튼으로 시험

## 8. 검토 결과 (2026-09-19 12:02, 상위 모델 리뷰)

**정할 것** — (a) `issues/*.md` 본문은 사람 소유, 봇은 머리말만 (→ #30 범위에 반영) · (b) 메시지 삭제는 자동 취소 말고 「취소/유지」 묻기 (→ #30)

**질문 답**
- bot.py 전면 분리 말고 순수 로직만 `core.py` 로 (score_of·place·balance·ref·need_hours·schedule) → #35
- 테스트는 `place()` 부터: 한도 초과 → 뒤로 / 완료 → 당김 / 사람 배정 유지 / 선행 미완료 대기 / 중복·단계 밖 → 나중
- cards.json 은 유지하되 `slack_ts`·`channel` 을 이슈 md 머리말에 → 재생성 가능한 캐시로. `save()` 는 임시 파일 + os.replace
- 봇의 main 직접 push 는 실험 단계에선 괜찮음

**확인된 결함** (→ #30): ~~A 커밋이 인덱스 전체 → `git commit -- <path>`~~ (9/19 고침, 시험 저장소로 확인) · B 보드 메타 매번 조회 → 캐시 · C 일괄 내보내기 카드당 push → 1회 · D GitHub 번호는 `webn77/slack-sandbox#N` 로만 · E push 실패 조용히 묻힘 → 로그

**코드 밖**: 성공 기준이 전부 사람에 대한 것인데 실사용자 0명 — 기능을 더 붙이기보다 사람 한 명을 넣어 잰다.

## 9. 근거
- 벤치마킹: `research/benchmark.md` — Tickr · Chaser · 플로우 리패턴 · 두레이 · 라인·삼성전자 기술 블로그 · PoC/파일럿 국내 용례
- 삼성전자: 수동 기록은 멈춘다 → 훅으로 자동 저장, 거르지 않고 다 남김, 팀 규칙을 같은 저장소에
- 라인: 계산·검증은 코드, LLM 은 검증된 숫자를 문장으로만

## 10. 2026-09-21 — 여러 프로젝트 · Ringo 와 나란히 재 보기

**닫은 것** #68 이슈 관계와 순서 · #30 bot.py 나누기 · #59 git 자동화+대조 · #66 프로젝트 표기 · #70 봇 하나가 프로젝트 여럿

**지금 자리** 카드 68 · 열린 32 (proj-sandbox 30 · proj-second 2) · 완료 15 · 바로 할 수 있는 일 21 · 테스트 **244개**

**여러 프로젝트가 실제로 돈다** — `webn77/pa-second` 레포와 `#proj-second` 채널을 만들어 재 봤다:
`SEC-1` · `SEC-2` 가 그 방에 뜨고 `webn77/pa-second#1·#2` 로 갔다. `PA-70` 과 번호가 안 겹친다.
앞말은 **레포**를 가리키고 프로젝트는 카드의 칸이다 — 한 프로젝트를 레포 둘로 쪼갤 수 있게 (교훈 1).
팀 대화방은 **없어도 되고** 프로젝트마다 다를 수 있다 (`store.req`) — 예전에는 없으면 봇이 아예 안 떴다.

**#68 의 원인은 예상과 달랐다** — 「관계 칸이 없다」 가 아니라 **AI 에게 다른 이슈를 안 보여 줬다.**
`ai.py` 프롬프트가 「점수 없는 이슈」만 보여 줘서, 혼자 매겨진 카드(로그상 34회)는 선행을 찾을 수가 없었다.
제목만 주면 0건, 「왜·바뀌는 것」 을 붙이면 15건 (실측). 선행 6 → 15건.

**Ringo 를 같은 워크스페이스에 붙여 놓고 직접 재 봤다** (`research/benchmark.md` 에 기록)
- **이번 주에 나왔다** (Product Hunt `@ringoai`, GPT-6 Astra, 「Seamless AX Co-worker」)
- **이슈도 만든다** — 다만 Jira·Linear 에 얹히고, 없으면 자기 「Ringo 투두」. 저장은 **Ringo 서버**
- 채널에서는 **묻지 않고 바로 만들고** 우선순위까지 붙인다. DM 에서는 담당자를 되물었다
- 앱 홈은 **안내판**이고 캔버스를 안 쓴다. 본거지가 DM 이다
- **전제가 다르다**: Ringo 는 트래커를 쓰는 팀, 우리는 트래커가 없는 팀

**우리가 부족한 것 넷을 문서에 적었다** — 남들에게 없는 것만 적으면 문서가 우리를 속인다:
① 대화 전체를 안 읽는다 ② 데일리 브리핑이 없다(#17) ③ 아무거나 물어보는 대화가 안 된다(**#71 로 고침**) ④ 설정을 화면에서 못 바꾼다

**RAG 는 지금 필요 없다** — 열린 31건이 4,672 토큰이라 **전부 넣는 게 가장 정확하다**
(#68 이 그 증거: 덜 넣어서 틀렸다). 밖의 문서(Notion·Drive)가 붙으면 그때 필요해진다 → **#72**.

**봇의 AI 는 도구·MCP·스킬이 전부 막혀 있다** (`--tools "" --strict-mcp-config`). 일부러다 —
Slack 메시지로 움직이니 열면 **누구나 이 맥을 건드린다.** 열려면 #14(안전 규칙)가 먼저다.
대신 **통로(`ask.jsonl`)로 밖에서 봇을 부르는 길**이 이미 있고, 오늘 하루가 전부 그 방식이었다.

**다음** #17 아침 알림(DM 이냐 채널이냐를 그때 정한다) · #52 나머지(반복 템플릿·비슷한 일에서 가져오기) ·
#64·#65 (Ringo 와 겹침 — 「이 이슈의 과거」 로 좁힐지 정할 것) · #44 개인 요청 입구 · #72 밖의 문서

## 11. 2026-09-21 오후 — 봇이 사는 자리 · 사람 말로 하기

사장님이 실제로 쓰시면서 나온 것들이다. 만들기 전에 **되물어 주신 덕에 안 만든 것**이 더 많다.

### 말을 사람 말로

- **「당겨오기」 는 내가 지어낸 말이었다.** `git pull` 이라는 이름이 이미 있는데 새 이름을 지으면
  검색도 안 되고 막혔을 때 뭘 찾아봐야 할지도 모른다. 사장님이 틀을 정해 주셨다 —
  **`(git 명령)` 을 앞에, 쉬운 말을 뒤에.**
- 「저장 안 된 파일」 도 나빴다 — 파일은 저장돼 있고 git 에 커밋이 안 된 것인데, 저렇게 쓰면
  **사장님이 자기가 뭘 안 저장했나 찾게 된다.**
- 「정리하고 봇을 다시 켜 주세요」 → **「동원님이 하실 일은 없어요」**. 봇 사정을 사람에게 시켰다.

### 켤 때 지금 버전을 알려 준다 (DM 으로)

받는 곳(`git remote`) · 지금 버전 · **누가 올렸는지**. 예전에는 막혔을 때만 말해서 조용할 때
어느 버전이 도는지 알 수 없었다. 프로젝트 방 전체가 보던 것을 **PM 에게 DM** 으로 돌렸다 —
봇을 켠 것은 한 사람인데 팀원에게는 손쓸 수 없는 소식이다.

**올린 이를 셋으로 나눴다** — 🤖 봇(`sandbox-bot`) · 🤖 Claude(`--author`) · 사람. Claude 가
`--author` 를 빠뜨리면 사장님 계정으로 찍혀 **둘이 한 사람처럼 보인다.** 규칙은 `docs/github-sync.md` §6.

### 말귀 (#71) — 사장님이 실제로 치신 말이 셋 다 빗나갔다

- **「내 할일」** — 목록에 「내 할 일」(다 띄움)·「내할일」(다 붙임)만 있고 **중간 형태가 빠져** 있었다.
  한국어는 띄어쓰기가 흔들린다 → 맞춰 보기 전에 양쪽에서 공백을 지운다
- **「찾을말」** — 안내문의 자리표시를 그대로 치셨다. **안내가 자기 함정을 놨다** → 칠 수 있는 예시로
- **같은 안내를 연달아 두 번** 봤다 → 처음 본 사람에게만 전부, 그 뒤엔 한 줄. 「도움말」 은 언제나 전부

### 봇이 사는 자리 (#10)

아침에 **봇이 두 대 떠서 25초 동안** 같은 Slack 연결에 붙어 있었다 (`pgrep -f "python3 bot.py"` 가
실제 명령줄 `Python bot.py` 와 안 맞아 kill 이 빈 PID 로 실패했는데 nohup 은 그대로 돌았다).
봇이 둘이면 **나중에 저장한 쪽이 이긴다** — `store.py` 가 `cards.json` 을 켤 때 한 번만 읽는다.

- **LaunchAgent 로는 「봇 하나」 가 안 된다** (사장님 지적). launchd 는 자기가 띄운 것만 관리한다
  → **파일 잠금(flock)**. 실측: 둘째가 나간 코드 1
- **LaunchAgent 등록** — 죽여도 14초 뒤 스스로 살아난다. `kill` 로는 이제 못 끈다(`bootout` 을 쓴다)
- **`bot.pid` 를 .gitignore 에** — 안 넣으면 `git pull` 이 더러운 트리로 보고 건너뛴다 (아침 버그와 같은 종류)

### 안 만든 것들 — 되물어 주신 덕이다

「업데이트는 왜 필요한 거야?」 · 「10은 재시작만 시키는 거 아니야?」 · 「무료인 거 없구나?」

- **`update` 명령**(코드 받고 갈아타기) — 지금은 **내가 맥에서 직접 재시작**하면 된다. 서버로 가면 그때
- **주기적 `git pull`** — `store.py` 가 한 번만 읽어서 **받아온 것을 옛 메모리가 덮는다.** 별도 이슈 크기
- **서버 이전** — `research/hosting.md`. Socket Mode 라 서버리스는 아예 안 되고, **Mac mini 는
  데스크탑이라 공짜로 24시간**이 된다. 「개인 컴퓨터라 문제」 는 **기종을 안 보고 한 말**이었다

### 내가 두 번 틀린 방식

**확인하지 않고 단정했다** — ① 기종을 안 보고 「개인 컴퓨터라 문제」 ② `pmset -g` 앞부분만 보고
「내가 막고 있어서 안 잔다」(실은 `sleep 0` 이 이미 설정돼 있었다). 둘 다 사장님이 바로잡으셨다.
**「~라서 문제입니다」 라고 말하기 전에 그 값을 먼저 찍는다.**

통로의 `item` 을 0 부터로 잘못 보내 **「재부팅 뒤 자동 실행 확인」 을 잘못 체크**했다 —
사장님이 직접 하셔야 하는 것이다. 봇을 내리고 되돌렸고, 되돌린 일도 기록에 남겼다.

## 12. 2026-09-23 밤 ~ 09-25 — 저장소 하나로 · 회의가 구글 캘린더에

### 코드는 moa 한 곳
코드가 두 벌(slack-sandbox · moa)이라 고칠 때마다 손으로 옮겼다(9/23 하루 6번). **코드는 moa, slack-sandbox 는 PA 팀
데이터만** 남겼다. 두 봇(PA · 모아)이 같은 코드 폴더에서 돌고 `MOA_DATA` 로 각자 데이터를 읽는다.
데이터는 한 바이트도 옮기지 않았다. 벤치마킹도 제품 조사라 moa 로 옮겼다(PA 쪽은 링크만).

### 회의 → 구글 캘린더 (#45)
- **Slack 이 유료로 하는 것을 무료 Slack 에서** — Slackbot 캘린더·스킬은 유료 플랜 전용이다. 모아가 그 자리
- 계정은 **사장님 기존 구글 계정**, 권한은 **앱이 만든 「모아」 캘린더만**(`calendar.app.created`, 「제한」 등급).
  심사 없이 통했다. 새 계정은 안 만들었다
- 구글 라이브러리 없이 REST 를 직접 부른다(`gcal.py`). 토큰은 데이터 폴더에만, `.gitignore`
- 정기 회의는 **반복 일정 하나** — 다음 회차 카드가 캘린더를 다시 만들지 않는다. 정기를 끄면 반복도 끝낸다
- OAuth 앱을 프로덕션으로 게시해 7일 만료를 없앴다. 게시에 공개 개인정보처리방침이 필요해 `PRIVACY.md` 를 두었다

### 회의 만들기가 바뀐 것
- **회의 카드는 항상 그 프로젝트 방** — 예전엔 요청 방(#팀-대화)으로 떨어졌다. 쓰지 않던 `meeting` 칸은 지웠다
- **⑤ 누구를 초대할까** — `@사람` · 1 모두 · 2 나만. 앞 칸에서 `@사람` 을 적으면 건너뛴다.
  초대 주소는 Slack 가입 이메일(두 앱에 `users:read.email` 추가·재설치)

### 실제로 써 보고 잡은 것
- 약칭이 빈 첫 프로젝트의 회의가 **DM 에 남았다** — `if key` 가 빈 글자를 「모름」 으로 읽었다
- `calendar.app.created` 로는 **캘린더 목록 조회가 403** — 연결이 거기서 죽을 뻔했다. 코드 검토 때 미리 막아 뒀다
- 매달 반복을 날짜(22일)로 두면 모아 카드와 **다른 날**이 된다 → 캘린더를 주차로 바꿨는데, **모아 쪽이 주차가 아니었다**
  (「4주 뒤, 같은 달이면 한 주 더」 — 9/22 넷째 화 다음이 10/20 셋째 화). 검토가 잡았다. 주차를 만들 때 정해
  `m["nth"]` 로 적어 두고 카드·캘린더가 같은 값을 쓴다. 1년치 날짜를 맞대는 시험을 넣었다
- 정기 회의 **둘째 회부터 시간이 비었다** — 다음 회 카드에 시간·프로젝트를 안 넘겨 10분 전 알림이 안 갔다
- ⑤ 에서 「그게 아니라 나만」 이면 **회의 이름이 「나만」** 이 됐다
- 연결 완료 화면의 **한국어가 깨졌다** — 인코딩을 알리지 않았다

### 내가 틀린 것
- 「`assistant.threads.setStatus` 가 2027-02 에 없어진다」 — 없어지는 건 `assistant_view` 였다. 문서를 끝까지 안 읽었다
- 운영용 모아 봇을 「시험용」 으로 읽었다 — `project.md` 제목이 설치 기본값 「모아 시험」 이었다. 사장님이 바로잡으셨다
- 9/23 커밋 셋에 `--author` 를 빠뜨렸다(이미 push 돼 그대로 둠)

남은 것: #76 켤 때 「지금 버전」 이 데이터 커밋을 보여 준다 · #77 상태 표시 새 API 로 옮길지 · 모아 워크스페이스는
**유료(Pro)** 라 Slackbot 캘린더와 직접 비교해 볼 수 있다

